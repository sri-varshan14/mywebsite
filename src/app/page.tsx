import SideBar from "@/components/common/SideBar"

export default function Home() {
    return (
        <main className="w-screen min-h-screen bg-base-100">
            <SideBar />
        </main>
    )
}
